import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { ProductService } from '../../services/product.service';import { CategoryService } from '../../services/category.service';import { Product, ProductPayload } from '../../models/product.model';import { Category } from '../../models/category.model';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<h1>Product CRUD</h1><div class="card formbox"><form class="form" (ngSubmit)="save()"><div class="grid two"><label class="field">Name<input name="name" [(ngModel)]="form.name" required></label><label class="field">Category<select name="category_id" [(ngModel)]="form.category_id" required><option *ngFor="let c of cats" [ngValue]="c.id">{{c.name}}</option></select></label></div><label class="field">Description<textarea name="description" [(ngModel)]="form.description"></textarea></label><div class="grid four"><label class="field">Price<input type="number" name="price" [(ngModel)]="form.price"></label><label class="field">Sale price<input type="number" name="sale_price" [(ngModel)]="form.sale_price"></label><label class="field">Stock<input type="number" name="stock" [(ngModel)]="form.stock"></label><label class="field">Image URL<input name="image_url" [(ngModel)]="form.image_url"></label></div><label><input type="checkbox" name="is_featured" [(ngModel)]="form.is_featured"> Featured</label><button class="btn btn-primary">{{editingId?'Update':'Create'}} product</button><button type="button" class="btn btn-light" (click)="reset()">Reset</button></form></div><div class="card" style="padding:20px;margin-top:20px"><table><tr><th>Name</th><th>Price</th><th>Stock</th><th></th></tr><tr *ngFor="let p of products"><td>{{p.name}}</td><td>{{(p.sale_price||p.price)|currency}}</td><td>{{p.stock}}</td><td><button class="btn btn-light" (click)="edit(p)">Edit</button> <button class="btn btn-danger" (click)="remove(p.id)">Delete</button></td></tr></table></div>`,styles:[`.formbox{padding:20px}.two{grid-template-columns:1fr 1fr}.four{grid-template-columns:repeat(4,1fr)}@media(max-width:900px){.two,.four{grid-template-columns:1fr}}`]})
export class ProductsAdminComponent implements OnInit{products:Product[]=[];cats:Category[]=[];editingId:number|null=null;form:ProductPayload=this.empty();constructor(private ps:ProductService,private cs:CategoryService){}ngOnInit(){this.load();this.cs.getAll().subscribe(c=>{this.cats=c;if(c.length&&!this.form.category_id)this.form.category_id=c[0].id;});}load(){this.ps.getProducts().subscribe(p=>this.products=p);}save(){const req=this.editingId?this.ps.update(this.editingId,this.form):this.ps.create(this.form);req.subscribe(()=>{this.reset();this.load();});}edit(p:Product){this.editingId=p.id;this.form={name:p.name,description:p.description,price:Number(p.price),sale_price:p.sale_price?Number(p.sale_price):null,stock:p.stock,image_url:p.image_url,category_id:p.category_id,is_featured:!!p.is_featured};}remove(id:number){this.ps.delete(id).subscribe(()=>this.load());}reset(){this.editingId=null;this.form=this.empty();if(this.cats.length)this.form.category_id=this.cats[0].id;}empty():ProductPayload{return{name:'',description:'',price:0,sale_price:null,stock:0,image_url:'',category_id:0,is_featured:false};}}
