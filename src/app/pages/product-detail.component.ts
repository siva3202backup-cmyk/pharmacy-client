import { Component,OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { ActivatedRoute,RouterLink } from '@angular/router';import { ProductService } from '../services/product.service';import { CartService } from '../services/cart.service';import { Product } from '../models';
@Component({standalone:true,imports:[CommonModule,RouterLink],template:`<section class="section"><div class="container"><a routerLink="/shop">← Back to shop</a><p class="alert err" *ngIf="error">{{error}}</p><div class="detail" *ngIf="product"><div class="bigpic">{{product.name[0]}}</div><div><span class="badge">{{product.category_name}}</span><h1>{{product.name}}</h1><p class="muted">{{product.description}}</p><h2 class="price">{{(product.sale_price??product.price)|currency}}</h2><p><b>Stock:</b> {{product.stock}}</p><button class="btn btn-primary" (click)="cart.add(product)">Add to cart</button></div></div></div></section>`,styles:[`.detail{display:grid;grid-template-columns:1fr 1fr;gap:34px;align-items:center;margin-top:22px}.bigpic{height:420px;border-radius:30px;background:linear-gradient(145deg,#f6fffc,#dffced);display:grid;place-items:center;font-size:150px;color:var(--p);font-weight:950}h1{font-size:42px}@media(max-width:780px){.detail{grid-template-columns:1fr}.bigpic{height:240px}}`]})export class ProductDetailComponent implements OnInit{product?:Product;error='';constructor(private route:ActivatedRoute,private ps:ProductService,public cart:CartService){}ngOnInit(){this.route.paramMap.subscribe(m=>{const id=Number(m.get('id'));this.ps.get(id).subscribe({next:p=>this.product=p,error:e=>this.error=e.error?.message||'Product not found'})})}}
