import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { ActivatedRoute, RouterLink, Router } from '@angular/router';import { FormsModule } from '@angular/forms';import { StoreService } from '../services/store.service';import { Product } from '../models';import { ProductCardComponent } from '../components/product-card.component';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink,ProductCardComponent],template:`<section class="section" *ngIf="product"><div class="container"><p><a routerLink="/shop">Shop</a> / <a [routerLink]="['/category',product.categorySlug]">{{product.category}}</a> / {{product.name}}</p><div class="detail"><div><div class="mainpic">{{product.image}}</div><div class="thumbs"><span>{{product.image}}</span><span>📦</span><span>🔍</span></div></div><div><span class="badge" *ngIf="product.prescription">Prescription required</span><h1>{{product.name}}</h1><p>Brand: <a [routerLink]="['/brand',product.brandSlug]">{{product.brand}}</a> · SKU: {{product.sku}}</p><p>★ {{product.rating|number:'1.1-1'}} ({{product.reviews}} reviews)</p><h2><span class="price">{{product.price|currency:'INR'}}</span> <span class="old">{{product.mrp|currency:'INR'}}</span></h2><p>{{product.description}}</p><p><b>Stock:</b> {{product.stock}} available</p><div class="row"><button (click)="qty=qty-1" [disabled]="qty<=1">-</button><input type="number" [(ngModel)]="qty" style="width:70px"><button (click)="qty=qty+1">+</button></div><div class="row"><button class="btn btn-primary" (click)="s.addCart(product,qty)">Add to cart</button><button class="btn btn-green" (click)="buyNow()">Buy now</button><button class="btn btn-light" (click)="s.toggleWishlist(product.id)">Wishlist</button><button class="btn btn-light" (click)="s.toggleCompare(product.id)">Compare</button></div></div></div><div class="tabs card"><h2>Description</h2><p>{{product.description}}</p><h3>Ingredients</h3><p>{{product.ingredients}}</p><h3>Dosage</h3><p>{{product.dosage}}</p><h3>Warnings</h3><p>{{product.warnings}}</p><h2>Reviews</h2><div class="review"><b>Good product</b><p>★ 5 - Packed well and delivered on time.</p></div></div><h2>Related Products</h2><div class="product-grid"><app-product-card *ngFor="let p of related" [product]="p"/></div></div></section>`,styles:[`.detail{display:grid;grid-template-columns:1fr 1fr;gap:34px}.mainpic{height:480px;background:#edf4f6;border-radius:10px;display:grid;place-items:center;font-size:170px}.thumbs{display:flex;gap:12px;margin-top:12px}.thumbs span{width:80px;height:80px;background:#edf4f6;border-radius:10px;display:grid;place-items:center;font-size:30px}.tabs{padding:24px;margin:32px 0}.review{border-top:1px solid var(--line);padding:12px 0}@media(max-width:900px){.detail{grid-template-columns:1fr}.mainpic{height:300px}}`]})export class ProductDetailComponent implements OnInit{product?:Product;related:Product[]=[];qty=1;constructor(private route:ActivatedRoute,public s:StoreService,private router:Router){}ngOnInit(){this.route.paramMap.subscribe(m=>{const p=this.s.product(Number(m.get('id')));if(p){this.product=p;this.related=this.s.related(p)}})}buyNow(){if(this.product){this.s.addCart(this.product,this.qty);this.router.navigateByUrl('/checkout')}}}
