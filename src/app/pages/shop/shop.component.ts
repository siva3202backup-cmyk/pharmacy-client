import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { ActivatedRoute, RouterLink } from '@angular/router';import { FormsModule } from '@angular/forms';import { ProductService } from '../../services/product.service';import { CartService } from '../../services/cart.service';import { Product } from '../../models/product.model';
@Component({standalone:true,imports:[CommonModule,RouterLink,FormsModule],templateUrl:'./shop.component.html',styleUrl:'./shop.component.scss'})
export class ShopComponent implements OnInit{products:Product[]=[];q='';category='';loading=false;constructor(private route:ActivatedRoute,private ps:ProductService,private cart:CartService){}ngOnInit(){this.route.queryParamMap.subscribe(v=>{this.q=v.get('q')||'';this.category=v.get('category')||'';this.load();});}load(){this.loading=true;this.ps.getProducts(this.q,this.category).subscribe({next:p=>{this.products=p;this.loading=false},error:()=>this.loading=false});}add(p:Product){this.cart.add(p);}}
