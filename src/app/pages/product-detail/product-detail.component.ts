import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { ActivatedRoute, RouterLink } from '@angular/router';import { ProductService } from '../../services/product.service';import { CartService } from '../../services/cart.service';import { Product } from '../../models/product.model';
@Component({standalone:true,imports:[CommonModule,RouterLink],template:`<section class="section"><div class="container" *ngIf="product"><a routerLink="/shop">← Back to shop</a><div class="detail"><div class="pic">{{product.name.slice(0,1)}}</div><div><span class="badge">{{product.category_name}}</span><h1>{{product.name}}</h1><p class="muted">{{product.description}}</p><h2 class="price">{{(product.sale_price||product.price)|currency}}</h2><p>Stock: {{product.stock}}</p><button class="btn btn-primary" (click)="cart.add(product)">Add to cart</button></div></div></div></section>`,styles:[`.detail{display:grid;grid-template-columns:1fr 1fr;gap:35px;align-items:center}.pic{height:420px;background:#effff9;border-radius:30px;display:grid;place-items:center;color:var(--primary);font-size:160px;font-weight:950}@media(max-width:760px){.detail{grid-template-columns:1fr}.pic{height:240px}}`]})
export class ProductDetailComponent implements OnInit{product?:Product;constructor(private route:ActivatedRoute,private ps:ProductService,public cart:CartService){}ngOnInit(){const id=Number(this.route.snapshot.paramMap.get('id'));this.ps.getProduct(id).subscribe(p=>this.product=p);}}
