import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { ActivatedRoute } from '@angular/router';import { StoreService } from '../services/store.service';import { Product } from '../models';import { ProductCardComponent } from '../components/product-card.component';
@Component({standalone:true,imports:[CommonModule,FormsModule,ProductCardComponent],template:`<section class="page-hero"><div class="container"><h1>Pharmacy Shop</h1><p>1000 products with filters, sorting, wishlist and compare.</p></div></section><section class="section"><div class="container shop"><aside class="card filter"><h3>Filter products</h3><label>Search<input [(ngModel)]="filters.q" (input)="apply()"></label><label>Category<select [(ngModel)]="filters.category" (change)="apply()"><option value="">All</option><option *ngFor="let c of s.categories" [value]="c.slug">{{c.name}}</option></select></label><label>Brand<select [(ngModel)]="filters.brand" (change)="apply()"><option value="">All</option><option *ngFor="let b of s.brands" [value]="b.slug">{{b.name}}</option></select></label><label>Min price<input type="number" [(ngModel)]="filters.min" (input)="apply()"></label><label>Max price<input type="number" [(ngModel)]="filters.max" (input)="apply()"></label><label><input type="checkbox" [(ngModel)]="prescription" (change)="apply()"> Prescription required</label><button class="btn btn-light" (click)="clear()">Clear</button></aside><main><div class="section-title"><h2>{{products.length}} Products</h2><select [(ngModel)]="filters.sort" (change)="apply()"><option value="new">Newest</option><option value="price-low">Price low to high</option><option value="price-high">Price high to low</option><option value="rating">Top rated</option></select></div><div class="product-grid"><app-product-card *ngFor="let p of pageProducts" [product]="p"/></div><div class="row pager"><button class="btn btn-light" [disabled]="page===1" (click)="page=page-1">Previous</button><span>Page {{page}} / {{pages}}</span><button class="btn btn-light" [disabled]="page===pages" (click)="page=page+1">Next</button></div></main></div></section>`,styles:[`.shop{display:grid;grid-template-columns:260px 1fr;gap:24px}.filter{padding:20px;display:grid;gap:14px;align-content:start}.filter label{display:grid;gap:6px;color:#184363;font-weight:700}.pager{justify-content:center;margin-top:24px}@media(max-width:900px){.shop{grid-template-columns:1fr}}`]})export class ShopComponent implements OnInit{filters:any={q:'',category:'',brand:'',min:null,max:null,sort:'new'};prescription=false;products:Product[]=[];page=1;perPage=24;constructor(public s:StoreService,private route:ActivatedRoute){}ngOnInit(){this.route.queryParamMap.subscribe(q=>{this.filters.q=q.get('q')||'';this.filters.category=q.get('category')||'';this.apply()})}apply(){this.filters.prescription=this.prescription;this.products=this.s.query(this.filters);this.page=1}clear(){this.filters={q:'',category:'',brand:'',min:null,max:null,sort:'new'};this.prescription=false;this.apply()}get pages(){return Math.max(1,Math.ceil(this.products.length/this.perPage))}get pageProducts(){return this.products.slice((this.page-1)*this.perPage,this.page*this.perPage)}}
