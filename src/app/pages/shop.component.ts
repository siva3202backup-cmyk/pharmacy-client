import { Component,OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { ActivatedRoute,RouterLink } from '@angular/router';import { ProductService } from '../services/product.service';import { CartService } from '../services/cart.service';import { Product } from '../models';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`<section class="section"><div class="container"><div class="section-title"><div><span class="badge">All categories</span><h1>Pharmacy Shop</h1><p class="muted">Search and open any product for details.</p></div><div class="search"><input [(ngModel)]="q" placeholder="Search products"><button class="btn btn-primary" (click)="load()">Search</button></div></div><p *ngIf="loading">Loading products...</p><div class="product-grid"><article class="card product" *ngFor="let p of products"><a class="pic" [routerLink]="['/products',p.id]">{{p.name[0]}}</a><p class="muted">{{p.category_name}}</p><h3><a [routerLink]="['/products',p.id]">{{p.name}}</a></h3><span class="price">{{(p.sale_price??p.price)|currency}}</span> <span class="old" *ngIf="p.sale_price">{{p.price|currency}}</span><p><button class="btn btn-primary" (click)="cart.add(p)">Add to cart</button></p></article></div></div></section>`})export class ShopComponent implements OnInit{q='';category='';products:Product[]=[];loading=false;constructor(private route:ActivatedRoute,private ps:ProductService,public cart:CartService){}ngOnInit(){this.route.queryParamMap.subscribe(m=>{this.q=m.get('q')||'';this.category=m.get('category')||'';this.load()})}load(){this.loading=true;this.ps.list(this.q,this.category).subscribe({next:p=>{this.products=p;this.loading=false},error:()=>this.loading=false})}}
