import { Component } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { Router } from '@angular/router';import { StoreService } from '../services/store.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<section class="section"><div class="container card login"><h1>My account</h1><p>Use admin@pharmacy.test to open admin menus.</p><form class="form" (ngSubmit)="login()"><label class="field">Email<input [(ngModel)]="email" name="email"></label><label class="field">Password<input type="password" [(ngModel)]="password" name="password"></label><button class="btn btn-primary">Login</button></form></div></section>`,styles:[`.login{max-width:440px;padding:28px}`]})export class LoginComponent{email='admin@pharmacy.test';password='Admin@123';constructor(private s:StoreService,private router:Router){}login(){this.s.login(this.email);this.router.navigateByUrl('/profile')}}
