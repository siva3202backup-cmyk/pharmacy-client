import { Component } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { Router,RouterLink } from '@angular/router';import { AuthService } from '../services/auth.service';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`<section class="section"><div class="container card auth"><h1>Login</h1><p class="muted">Admin: admin@pharmacy.test / Admin@123</p><p class="alert err" *ngIf="error">{{error}}</p><form class="form" (ngSubmit)="submit()"><label class="field">Email<input type="email" name="email" [(ngModel)]="email" required></label><label class="field">Password<input type="password" name="password" [(ngModel)]="password" required></label><button class="btn btn-primary">Login</button></form><p>No account? <a routerLink="/register">Register</a></p></div></section>`,styles:[`.auth{max-width:450px;padding:28px}`]})export class LoginComponent{email='admin@pharmacy.test';password='Admin@123';error='';constructor(private auth:AuthService,private router:Router){}submit(){this.error='';this.auth.login({email:this.email,password:this.password}).subscribe({next:()=>this.router.navigateByUrl('/dashboard'),error:e=>this.error=e.error?.message||'Login failed. Check API and PostgreSQL connection.'})}}
