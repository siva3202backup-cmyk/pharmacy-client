import { Component } from '@angular/core';import { CommonModule } from '@angular/common';import { StoreService } from '../services/store.service';import { ProductCardComponent } from '../components/product-card.component';
@Component({standalone:true,imports:[CommonModule,ProductCardComponent],template:`<section class="page-hero"><div class="container"><h1>Wishlist</h1></div></section><section class="section"><div class="container"><div class="product-grid"><app-product-card *ngFor="let p of products" [product]="p"/></div><p *ngIf="!products.length">No wishlist products yet.</p></div></section>`})export class WishlistComponent{constructor(public s:StoreService){}get products(){return this.s.products.filter(p=>this.s.wishlist().includes(p.id))}}
