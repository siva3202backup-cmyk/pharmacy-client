import { Component } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { Router, RouterLink } from '@angular/router';import { AuthService } from '../../services/auth.service';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`<section class="section"><div class="container auth card"><h1>Create account</h1><div class="alert alert-error" *ngIf="error">{{error}}</div><form class="form" (ngSubmit)="submit()"><label class="field">Name<input name="name" [(ngModel)]="name" required></label><label class="field">Email<input type="email" name="email" [(ngModel)]="email" required></label><label class="field">Password<input type="password" name="password" [(ngModel)]="password" required minlength="6"></label><button class="btn btn-primary">Register</button></form><p>Already have account? <a routerLink="/login">Login</a></p></div></section>`,styles:[`.auth{max-width:460px;padding:30px}`]})
export class RegisterComponent{name='';email='';password='';error='';constructor(private auth:AuthService,private router:Router){}submit(){this.auth.register({name:this.name,email:this.email,password:this.password}).subscribe({next:()=>this.router.navigateByUrl('/dashboard'),error:e=>this.error=e.error?.message||'Registration failed'});}}
