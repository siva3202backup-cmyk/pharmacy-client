import { Component } from '@angular/core';import { CommonModule } from '@angular/common';import { RouterLink } from '@angular/router';import { StoreService } from '../services/store.service';
@Component({standalone:true,imports:[CommonModule,RouterLink],template:`<section class="page-hero"><div class="container"><h1>My Orders</h1></div></section><section class="section"><div class="container card" style="padding:20px"><table><tr><th>Order</th><th>Date</th><th>Status</th><th>Payment</th><th>Total</th></tr><tr *ngFor="let o of s.orders()"><td><a [routerLink]="['/orders',o.id]">{{o.number}}</a></td><td>{{o.date|date}}</td><td>{{o.status}}</td><td>{{o.paymentMethod}} / {{o.paymentStatus}}</td><td>{{o.total|currency:'INR'}}</td></tr></table><p *ngIf="!s.orders().length">No orders yet.</p></div></section>`})export class OrdersComponent{constructor(public s:StoreService){}}
