# MediMart Pharmacy Angular 20 Client

## Run
```bash
npm install
npm start
```
The app expects the API at `http://localhost:5000/api`. Update `src/environments/environment.ts` if needed.

Default admin from server seed: `admin@pharmacy.test` / `Admin@123`.
